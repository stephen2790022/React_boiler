const initialState = {
    userData: []
}

export const reducer = (state = initialState, action) => {
    switch (action.type) {
        case 'value':
            break;
    
        default:
            break;
    }
}

